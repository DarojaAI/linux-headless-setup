# 2026-06-14 — Q13 PR triage (skill-bridge + dev-nexus-frontend)

Operator: "fix yourself, and triage stale prs."

## skill-bridge

### #14 — release.yml references a 404 workflow
**My Q13 sweep was wrong** — the file was already fixed in PR #20 (merged 2026-06-14 01:13 UTC). The current `release.yml` points to `DarojaAI/infra-actions/.github/workflows/reusable-semantic-release.yml@main`, which is the correct path.

**Action taken:** Closed issue #14 with a comment pointing at PR #20. Lesson logged: I should have read the file before reporting it as a release blocker.

### #28 — `src/manifest/init.ts` writes `$schema` to `v1.json` (404)
**Still valid.** The shipped manifest was updated to v2 in PR #27 (closes #16), but the `initManifest()` generator kept writing the broken URI. So any new consumer running `skill-bridge init --target openclaw` would get a manifest with a 404 `$schema` reference.

**Action taken:** Fix in PR #31. Single-line change: `v1.json` → `v2.json`. 115/115 vitest tests pass.

### #19 — Flip `update-docs: true` in release.yml once `infra-actions#85` merges
**Now unblocked.** `infra-actions#85` (the opt-in `update-docs` step) was merged 2026-06-13. Just needs the `update-docs: true` and `docs-files: README.md,docs/ARCHITECTURE.md` flags added to `release.yml`.

**Action taken:** Included in PR #31. The docs/files list matches the renamed PLAN.md → docs/ARCHITECTURE.md from PR #20.

### #11 — Shipped `skill-bridge.manifest.json` looks like a deployment artifact
**Not addressed.** Open question. The shipped manifest points at linux-desktop-seed's layout (outDir, local source path, PROPRIETARY skills owned by Milan Patel). This is a consumer manifest masquerading as a self-describing example. Three options:

1. Move to `examples/consumer-manifest.json` and replace the root file with a self-describing example.
2. Move to linux-desktop-seed where it semantically belongs.
3. Leave as-is (the operator's deployment, the operator's repo, both are right to live together).

Operator decision needed. Not addressed in PR #31.

## dev-nexus-frontend

### #48 (89d) — feat: GCP readiness frontend implementation
**Authored by `@patelmm79`** (the operator). 2 files, +18/-14. All checks pass (claude-review ✓, Vercel preview ✓).

**Status:** Ready to merge from a code perspective. The 89-day age is a smell — feature may have shifted. Recommendation: **rebase/merge**, but worth a re-read of the feature context to confirm it's still wanted.

**Action taken:** None yet. The PR is operator-authored; I'll let the operator decide whether to merge. If you say "merge it," I'll rebase and merge, or just approve.

### #60 (61d) — Add Cloudflare Workers configuration (cloudflare bot)
**Bot PR from Cloudflare's autoconfig flow.** 5 files, +4227/-2793 (huge — includes pnpm-lock).

**CI is failing** but the failure is **pre-existing**, unrelated to this PR:
- Errors: `'penpot' is not defined` (no-undef), `'console' is not defined` (no-undef), `'idx' is defined but never used` (no-unused-vars) — in `penpot-create-wireframes.js` and `test-wire.js`, both **already in main**.
- The Cloudflare PR doesn't touch those files.

**Also:** `wrangler.toml` is **already in main** (a different file in the same family). So this PR may be **superseded** by an earlier merge of similar config.

**Recommendation:** **Close as superseded.** The Cloudflare config is already adopted via a different path (`feat/cloudflare-deploy-workflow` branch exists, and `wrangler.toml` is in main). The 4227-line diff is mostly lock-file churn that we don't need.

**Action taken:** None yet. If you say "close it," I'll comment + close with a "superseded" message.

## Other findings from the dev-nexus-frontend repo (separate from PRs)

The repo has accumulated a lot of session output and phase docs at the root:
- `*PHASE_2*.md`, `*PHASE_3*.md`, `*V2_MVP*.md`, `PENPOT_*.md`, `*WIREFRAME*.md`, `*EXEC*.js`, `SESSION_FINAL_SUMMARY.md`, `EXTENDED_SESSION_FINAL.md`
- These look like agent session dumps, not living docs
- Worth a single housekeeping PR that moves them to `memory/` or `archive/`

**Not addressed** — outside the operator's "triage stale PRs" scope. Worth flagging for a future housekeeping sweep.

## PRs I opened this turn

- `DarojaAI/skill-bridge#31` — fix: init.ts v2 schema + release.yml update-docs

## Issues I closed this turn

- `DarojaAI/skill-bridge#14` — release.yml 404 (already fixed by PR #20)

## Operator decision still needed

- #48: merge or close?
- #60: close (superseded) or hold?
- skill-bridge#11: where does the deployment manifest live?
- dev-nexus-frontend root cleanup: housekeeping PR or not?
