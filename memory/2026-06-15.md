# 2026-06-15 — Pre-compaction memory flush

## Operator state

Operator: `no_decaf_milan` (Milan Patel). Working on the Cognee orbit convergence. Last user-visible message in this session was approving sign-off on the convergence plan and asking me to file issues. That work landed:

- **Plan merged** — `DarojaAI/rag_research_tool/docs/convergence-plan.md` (PR #813) MERGED.
- **6 new issues filed** in the correct repos:
  - `DarojaAI/rag_research_tool#814` — P0: pipeline bypasses canonical store
  - `DarojaAI/rag_research_tool#815` — P1: archive old CLI scripts (two parallel pipelines)
  - `DarojaAI/rag_research_tool#816` — Convergence meta-issue
  - `DarojaAI/rag_research_tool#817` — Naming decision
  - `DarojaAI/research-orchestrator#1` — Schema mismatch + sys.path deprecation
  - `DarojaAI/intelligent-feed#2` — Make installable Python package
- **Existing #789** got a full-diagnosis comment.
- **Architect repo** at `cb4261e` on `docs/rag-research-tool-take`. Deep-dive take got a status block pointing to the new issues.

## End-of-session reflection (Hermes nudge)

### 1. Gaps between assumption and reality

- I assumed the orchestrator's `rag_research_tool` path had run end-to-end at least once. It hasn't — the schema mismatch has been silent for 6 weeks (last commit 2026-05-03, zero open issues/PRs). The activation is wired but the input shape is wrong, so `c.get("entity_type")` AttributeErrors on first real call.
- I assumed the architect repo's deep-dive would naturally cross-link to issues in the target repos. It didn't — the link is implicit ("see #789") but the actual issue numbers didn't exist when the take was written. **Lesson:** when filing issues, the take doc and the issues should be created in the same pass, not in different sessions.
- I assumed PR #3 in the architect repo was open at `main`. It's open at `docs/rag-research-tool-take` — operator never asked me to merge it.

### 2. First-time things (candidate skills)

- **Filing an issue in a target repo from the architect repo**: I have a clear shape now (problem → evidence with file:line → proposed fix with file list → acceptance criteria → risk → related issues/plans). This is a reusable pattern. Could be a skill: `cross-repo-issue.md`.
- **Cross-linking issues to a plan doc**: I appended a status block to the deep-dive take. Could be standardized as part of `cross-repo-issue.md`.
- **Diagnosing "I assumed the integration was working but it was silently broken for 6 weeks"**: not unique to this case. The pattern is: find a contract (Pydantic schema, API shape, env contract), find the consumer, find the producer — check the wire shape. Skill: `verify-cross-repo-contract.md`.

### 3. Surprises about how I worked

- **Operator said "save the convergence plan" — I saved in the target repo, not the architect repo.** Correct call. The plan lives in the repo it documents. The architect repo gets the deep-dive meta-doc, not the plan. This separation is correct but worth being explicit about in `ARCHITECTURE.md` or `AGENTS.md`.
- **I created a new branch from `main` in `rag_research_tool` (`docs/convergence-plan`)** without checking if there was a CONTRIBUTING.md requirement. The PR was accepted (no CONTRIBUTING.md constraint). Future branch names should be lowercase-kebab, no `feat/` prefix (org convention).
- **I opened PRs and issues in 3 different repos in a single turn** — that's appropriate scope, but I should batch-verify at the end (I did do `git status` checks; the verify step caught the issue-number reservation problem in the issue bodies — I had to re-file with correct numbers).

### 4. Still unclear (gaps → OPEN_QUESTIONS.md)

- **Operator choice on the naming decision** (Synapse vs rag_research_tool vs FortEvent). I recommended Synapse but didn't get sign-off. Tracked in `DarojaAI/rag_research_tool#817`.
- **Which issue to attack first** (Phase A.1 = 1-line #789 fix is the cheapest; Phase A.3 = postgres_write stage is the highest-value). Operator hasn't said.
- **Intelligent-feed package distribution** (PyPI public vs GCP Artifact Registry private). Affects `intelligent-feed#2` decision.
- **rag_research_tool rename to synapse** (if we go Synapse): Cloud Run service rename is a separate change with operational implications (DNS, IAM, monitoring).
- **The dynamic-worlock 404** (Q14 from MEMORY.md) is still unresolved. Confirmed it doesn't exist; still need operator confirmation.
- **DarojaAI/research-orchestrator#1** is the first issue ever filed in that repo — the org just woke up a sleeping repo. Worth noting that this is a "reactivation" event, not normal issue flow.

### 5. Load-bearing facts (promote to MEMORY.md if true a month from now)

- **The Cognee orbit (rag_research_tool + research-orchestrator + intelligent-feed) converges at Postgres `entities`/`relationships` tables.** Per `rag_research_tool/docs/ARCHITECTURE.md`. Load-bearing for the convergence plan.
- **The activation contract between research-orchestrator and intelligent-feed is broken.** Silently, for 6 weeks. **Pattern lesson:** every cross-repo Pydantic/dataclass contract needs a contract test, not just a doc. Promotable to MEMORY.md §"Standards cheat sheet".
- **Operator's naming preference: not yet known.** Three names in flight in rag_research_tool (rag_research_tool, Synapse, FortEvent). Tracked in #817.
- **The "shared lib with sys.path injection" pattern is in the org at least twice** (devnexus-common Q5, intelligent-feed D.1). The fix shape is the same: make it a real installable package. **Third instance of the same fix shape — this is a pattern, not a coincidence.** Promotable to MEMORY.md §"Recurring traps" with a strong recommendation: any future shared lib should be packaged from day 1.

## Promotion candidates for MEMORY.md (next curator pass)

1. **The activation contract pattern:** "Cross-repo Pydantic/dataclass contracts need contract tests, not just docs." — Add to §"Standards cheat sheet" or §"Recurring traps".
2. **The "shared lib" pattern recognition:** "Any repo imported via env var + sys.path injection in another repo is a packaged-lib-to-be. Fix shape: pyproject.toml + install + requirements pin + version constraint." — Add to §"Recurring traps" or §"Load-bearing facts" as a new subsection.
3. **Issue cross-linking rule:** "When filing issues across repos for a cross-repo plan, file the issues and link from the plan in the same pass. Otherwise the plan and the issues drift out of sync." — Add to §"How I work" in AGENTS.md.
4. **Architect repo vs target repo for plans:** "Plans live in the target repo (the one they document). The architect repo gets the meta-doc / deep-dive take, not the plan itself." — Add to §"How I work" in AGENTS.md.
5. **Reactivation events are different from normal issue flow:** "If a repo has been silent for 6+ weeks and you're filing the first issue, flag it as a reactivation — the owner may be surprised." — Add to §"How I work" in AGENTS.md.

## Open issues log update (will copy to OPEN_QUESTIONS.md next session)

- Q15: Operator decision on naming (Synapse vs rag_research_tool vs FortEvent). Issue: DarojaAI/rag_research_tool#817.
- Q16: Operator decision on first issue to attack (A.1 = #789 vs A.3 = postgres_write). Plan: DarojaAI/rag_research_tool/docs/convergence-plan.md.
- Q17: Intelligent-feed distribution channel (PyPI public vs GCP Artifact Registry private). Issue: DarojaAI/intelligent-feed#2.

## Status snapshot

- **3 repos touched today:** `DarojaAI/rag_research_tool`, `DarojaAI/research-orchestrator`, `DarojaAI/intelligent-feed`.
- **Architect repo at `cb4261e`** on branch `docs/rag-research-tool-take`. PR #3 still open (not asked to merge).
- **All writes verified** — `git status` clean, files re-read, issue numbers match the issues I filed.
- **All issues filed with `enhancement` label** (the only safe label that exists on these repos). The cross-repo org labels (`P0`, `P1`) don't exist — promotable as a "org needs richer label taxonomy" suggestion to `.github` RFC.

## Things I want to remember for the next session

- The convergence plan is the operator's first end-to-end cross-repo convergence. It's a template for future multi-repo work.
- The 6 issues form a graph: meta (#816) → 4 in rag_research_tool (#789, #814, #815, #817) + 1 in research-orchestrator (#1) + 1 in intelligent-feed (#2). Phase A.1 (#789) is the cheapest unblock. Phase A.3 (#814) is the highest-value.
- The `dev-nexus`/`mcp-tooling`/`openclaw-gateway` triad boundary contract is **still** the most rigorous P0 doc in the org. Re-read it for future cross-repo work.
- The org has **3** "shared lib to be" repos (devnexus-common, intelligent-feed, and the future 5th-category-shared-libraries repos). The pattern needs a skill.
- The "first issue in a sleeping repo" pattern is real — research-orchestrator went from 0 issues to 1 in one turn.

## Reflection meta

- The pre-compaction flush is happening in the middle of a quiet operator period (3:06 AM UTC, operator likely sleeping). The right call is: capture everything to disk, NO_REPLY the runtime event, and wait for the operator to wake up and choose the next action.
- This is the second pre-compaction flush tonight (per the runtime hint). Workspace state should be stable. All open PRs and issues are visible to the operator when they return.

---

## Update 03:13 UTC — rag-research-tool agent pushback

Operator pasted a response from the in-repo agent. **They were mostly right.** I had read the docs/ARCHITECTURE.md "Remaining Gaps" table at face value and didn't verify against the actual code. Corrected understanding:

### What I got wrong

1. **#814 (P0: pipeline bypasses canonical store) is wrong.** Stage 2 already calls `TripletVerifier` → `PostgresWriter.write_triplets()` (`tools/pipeline/triplet_verifier.py:23, 95`). `postgres_writer.py` is **not orphan code** — it's wired in.
2. **"Neo4j syncs from raw triplets" is wrong.** Stage 2.5 (`pipeline_orchestrator.py:729-731`) calls `sync_postgres_to_neo4j()` which reads from `rag_research_bronze.entities`/`.relationships`. The sync is from Postgres, not from raw triplets.
3. **#789 fix was wrong.** I suggested 1-line rename to `source_documents`. Operator's actual fix: created a separate `documents` table (`20260614000000_create_documents.sql`) and `claim_versions` table. The router was always correct; the schema was missing. **Lesson:** the FastAPI `documents` table is distinct from the canonical `source_documents` table. Don't conflate them.
4. **C.1 (shared schema unification) is smaller than I claimed.** Schema already has `entity_type`, `rel_type`, `source_entity_id`/`target_entity_id`. The real delta is naming reconciliation between intelligent-feed's claim shape and rag's triplet shape, not a new schema.
5. **P1b (two parallel pipelines) is overstated.** Old `src/rag/run_kg_pipeline.py` is probably orphaned, not parallel. Need grep against routers + CI to confirm.
6. **Naming surface area is under-counted.** Cloud Run `rag-research-eai` is one of several surfaces; there are likely customer-facing strings and skill names I haven't found.
7. **Migration scar tissue:** `20260603000000` + `20260604000000` (v1 + v2 of `source_document` relationship). "Additive, forward-only" is more delicate than my plan implies.

### What's still right

- The plan's shape (5 phases) is right even if A and C shrink.
- Convergence at Postgres is the right architectural bet.
- `research-orchestrator#1` is real (silent P0 in a separate repo, can't be confirmed without the agent's repro).
- The cross-repo convergence goal is the right goal.

### Lesson (promotable to AGENTS.md)

**Verify the code, not the docs.** `docs/ARCHITECTURE.md` "Remaining Gaps" table said the pipeline "does not write to Postgres `entities`/`relationships`." I trusted it. The code (TripletVerifier.write_triplets + sync_postgres_to_neo4j) shows it does. **The doc was stale relative to the code.** When diagnosing, read both and reconcile. If they conflict, the code wins. Update the doc as part of the fix.

### Status of issues/PRs after this turn

- **#789 CLOSED** (operator's work: added the documents + claim_versions migrations)
- **intelligent-feed#2 CLOSED** (operator's work: packaged the lib with pyproject.toml + setuptools_scm + Apache-2.0 license + PyPI)
- **#814, #815, #816, #817 OPEN** in rag_research_tool
- **research-orchestrator#1 OPEN**
- **PR #813 (convergence-plan) MERGED** in rag_research_tool
- **PR #3 (deep-dives) OPEN** in darojaai_architect (not asked to merge)
- **Q14 RESOLVED**: dynamic-worlock is private (operator-confirmed in `e16af80` commit on intelligent-feed)

### What I'm waiting for

Operator pick: (a) corrective PR, (b) e2e test first, (c) have the agent produce orchestrator repro first. My pick is (c) e2e test first — it's a 30-second `pytest` and gives ground truth.

### Other deltas this turn

- `intelligent-feed` is now on PyPI: https://pypi.org/project/intelligent-feed/ (operator-published)
- License changed from MIT (Q15-17 PR body) to Apache-2.0 (the actual packaging commit). **Note for MEMORY:** the org has switched to Apache-2.0 for this lib. Check if that affects the py-daroja-libs rename.
- `requires-python = ">=3.14"` is a hard requirement on intelligent-feed. That's ahead of what the rest of the org probably runs.
- `setuptools_scm` is used (dynamic version). Means the version comes from git tags, not a hardcoded number.
