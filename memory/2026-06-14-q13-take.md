# 2026-06-14 — Q13 sweep (stale branches / open PRs / open issues)

Operator: "do q13" (after green-lighting items 1, 2, 3, 4, 5, 6, 9, 10, 11, plus this turn 7/13).

## Headline numbers

- 18 open PRs across 8 repos
- 65+ open issues across 17 repos
- 15 "quiet" repos (0 PRs, 0 issues)

## The critical finding — `skill-bridge#14`

`.github/workflows/release.yml` references a path that returns 404:

```yaml
uses: DarojaAI/dev-nexus/.github/workflows/zzz-reusable-semantic-release.yml@main  # 404
```

**Any push to main right now would fail to release.** The correct path is `DarojaAI/infra-actions/.github/workflows/reusable-semantic-release.yml`. I opened this issue in session 9 but the title is now confirmed. **Highest-priority item in this report.** Should be the next PR merged into `skill-bridge`.

## Stale PRs (>30 days)

| Repo | PR | Age | Title |
|---|---|---|---|
| `dev-nexus-frontend` | #48 | 89d | feat: GCP readiness frontend implementation |
| `dev-nexus-frontend` | #60 | 61d | Add Cloudflare Workers configuration |

Both authored by `@patelmm79` or bot. Worth a sweep: review, merge, or close.

## Repos with ≥8 open issues (the backlogs)

| Repo | Open issues | Notes |
|---|---|---|
| `core-business-management` | 10 | LLC formation steps (55d old); many `[DEFERRED]`/`[CONTINGENT]` |
| `GlobalBitings` | 9 | **All 74d old.** P0/P1 data-collection work. Possibly stalled. |
| `dev-nexus` | 9 | DB Fortification epic (6 issues, 31d) + 3 newer |
| `rag_research_tool` | 8 | Mix of infra, MLflow, GCS governance, recent fixes |

## Recent open PRs (the version-bump stream)

- `devnexus-common` has 5 open PRs, all version bumps, 0-3 days old
- `gcp-dbt-terraform`, `gcp-postgres-terraform`, `gcp-vpc-egress-terraform` — version bumps, 6 days old

This is the **semantic-release** workflow. The bot creates the PR; the operator (or the merge action) approves it. Not a problem, just a pattern.

## 15 "quiet" repos (0 PRs, 0 issues)

**Intentionally quiet** (templates, examples, mirrors):
- `linux-desktop-seed-public`, `gcp-postgres-terraform-example`, `gcp-postgres-validators`, `daroja-frontend-starter`, `rag-research-tool-frontend`, `terraform-gcp-cloudrun-vpc-job`, `business-website`, `design-artifacts`

**Possibly quiet because they're stable:**
- `terraform-hcloud-linux-vm`, `terraform-gcp-wrappers`, `bond-nexus`, `machine-learning-commons`, `vpc-infra`, `linux-headless-setup`

**Possibly stale (worth a follow-up):**
- `research-orchestrator` (1 workflow, no infra-actions)
- `mcp-tooling` (1 workflow, no infra-actions)
- `openclaw-gateway` (1 workflow, no infra-actions, no CODEOWNERS — multiple Q6/Q7 gaps)
- `darojaai_architect` — me; intentionally quiet

## Recommended actions (ordered by priority)

1. **Fix `skill-bridge#14` (release.yml 404).** Release blocker.
2. **Triage `dev-nexus-frontend#48, #60`.** 89d and 61d old.
3. **`GlobalBitings` 9 stale issues (74d).** Sweep "still relevant?"
4. **`core-business-management` 10 issues (55d).** Close `[DEFERRED]` to clarify the live list.
5. **`dev-nexus` DB Fortification epic.** Single "is this still on?" check.

## Caveats

- Branch ages not surveyed in depth. Heavy-branch repos: `linux-desktop-seed` (100), `dev-nexus` (79), `rag_research_tool` (48), `gcp-postgres-terraform` (43).
- Issue / PR template compliance not surveyed.
- Issue close rate over time not analyzed (would need historical data).

Raw data: `_context/audits/q13/per-repo-stats.txt` and `_context/audits/q13/REPORT.md`.
